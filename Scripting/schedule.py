# Name: Daniel Chen
# Date: 6 March 2019
# Description: Asks user for name, class, and period of class and outputs it in "<name> has <class> in period <period>." format

name = input('Name: ')
course = input('Course name: ')
period = input(course + ' period number: ')
print(name + ' has ' + course + ' in period ' + period + '.')